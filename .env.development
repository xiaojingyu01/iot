# 网关地址（若依 ruoyi-gateway 默认 8080）；直连 IoT 调试改为 http://127.0.0.1:9204 且 VITE_API_PREFIX 置空
#
# 真机预览：不要用 127.0.0.1（手机访问的是自己）。任选其一：
# ① 把下面改成穿透根地址后重新编译，例如 VITE_API_BASE_URL=https://xxx.ngrok-free.app
# ② 保持 127.0.0.1，在手机小程序「我的 → 接口与穿透设置」里填写同一穿透地址（无需重编）
#
# 微信：须「不校验合法域名」或公众平台配置 request 合法域名；本项目 project.private.config.json 已 urlCheck:false 便于本地预览。
# 花生壳等内网穿透：填 https://你的域名（不要带 /iot-app）。
VITE_API_BASE_URL=http://127.0.0.1:8080
# 与 Nacos 中小程序路由前缀一致（RewritePath 会剥掉此前缀）；走网关时不要留空
VITE_API_PREFIX=/iot-app
# 头像/静态文件：上传接口返回 http://内网:9300/statics/... 时，小程序 <image> 需 HTTPS。填穿透后的文件根（无尾斜杠），如 https://xxx.vicp.fun:9300
# VITE_FILE_PUBLIC_ORIGIN=https://你的文件穿透域名:9300
VITE_FILE_PUBLIC_ORIGIN=https://1yx22cf258916.vicp.fun